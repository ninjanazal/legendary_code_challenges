#include "questionsWithBrainPower.h"

#include <unordered_map>
#include <vector>

#include "profileBlock.h"

using namespace LendaryCodeChallenges;
using namespace std;

void QuestionsWithBrainPower::execute() {
    printf("--- Uncrossed Lines ---\n");
    vector<vector<int>> input = {{3, 2}, {4, 3}, {4, 4}, {2, 5}};
}

long long QuestionsWithBrainPower::mostPoints(vector<vector<int>>& questions) {
    long long output= 0;

    for (int i = 0; i < questions.size(); i++){
        
    }

    return output;
}

void QuestionsWithBrainPower::printResult(long long result) {
    std::cout << result << std::endl;
}
